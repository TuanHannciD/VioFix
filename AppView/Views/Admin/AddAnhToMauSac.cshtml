@model IEnumerable<AppData.ViewModels.SanPham.AnhRequest>

@{
    ViewData["Title"] = "AddAnhToDungTich";
}

<h1>AddAnhToDungTich</h1>

<form enctype="multipart/form-data" asp-action="AddAnhToDungTich">
    <table class="table">
        <thead>
            <tr>
                <th>
                    Chọn ảnh
                </th>
                <th>
@*                     @Html.DisplayNameFor(model => model.TenDungTich)
 *@                </th>
                <th>
@*                     @Html.DisplayNameFor(model => model.MaDungTich)
 *@                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <input type="file" name="images" />
                    </td>
                    <td>
@*                         @Html.DisplayFor(modelItem => item.TenDungTich)
 *@                    </td>
                    <td>
@*                         @Html.DisplayFor(modelItem => item.MaDungTich)
 *@                    </td>
                    <td>
@*                         <input type="text" name="idDungTichs" value="@item.IDDungTich"/>
 *@                    </td>
@*                     <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td> *@
                </tr>
            }
        </tbody>
    </table>
    <button class="btn btn-primary" type="submit">Submit</button>
</form>

